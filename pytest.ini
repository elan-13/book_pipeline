[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    unit: Unit tests for transformations and helper functions
    mock: Mock tests for external API and database boundaries
    quality: Data validation gate and schema tests
